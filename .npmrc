# Dev lokal: jangan jalankan cek/auto-install sebelum `pnpm run` agar script tetap
# jalan walau node_modules segar dipasang. CI memakai install fresh; persetujuan
# build esbuild sudah tersimpan di pnpm-workspace.yaml (allowBuilds/onlyBuiltDependencies).
verify-deps-before-run=false
